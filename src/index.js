import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import App from "./App"; //please look the same folder index.js file exists

//here we are telling render app component in the root element of the document
ReactDOM.render(<App />, document.getElementById("root"));
